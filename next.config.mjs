/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['lh3.googleusercontent.com', 'nasraschool.edu.pk', 'encrypted-tbn0.gstatic.com']
    }
};

export default nextConfig;
